type CityLocation = {
  title: string;
  lat: number;
  lng: number;
  zoom: number;
};

type PointOfferLocation = {
  title: string;
  lat: number;
  lng: number;
  zoom: number;
  id: string;
}
export type {CityLocation, PointOfferLocation};
